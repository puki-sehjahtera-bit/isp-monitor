{
  "name": "isp-monitor",
  "version": "2.0.0",
  "description": "ISP Monitor — Laptop self-host (Docker)",
  "type": "commonjs",
  "main": "worker/index.mjs",
  "scripts": {
    "test": "node --test",
    "start": "node worker/server.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "node-cron": "^3.0.3"
  },
  "devDependencies": {}
}
