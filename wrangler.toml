name = "isp-monitor"
pages_build_output_dir = "public"
compatibility_date = "2024-09-23"

# Frontend statis murni (Cloudflare Pages).
# Deploy otomatis dari GitHub: output = folder public/.
# API di domain terpisah: https://api.isp-monitor.my.id
# API_BASE/WS_URL diisi di public/config.js.

[vars]
# Proxy /api/* (functions/api/[[path]].js) tuju Worker API.
API_WORKER_URL = "https://isp-monitor-api.imambaru2001.workers.dev"
